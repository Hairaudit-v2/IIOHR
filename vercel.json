{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -f iiohr-next/package-lock.json ]; then npm ci --prefix iiohr-next; else npm ci; fi",
  "buildCommand": "if [ -f iiohr-next/package.json ]; then npm run build --prefix iiohr-next; else npm run build; fi"
}
